diff --git a/partner/dpi/gstreamer/CMakeLists.txt b/partner/dpi/gstreamer/CMakeLists.txt
index 9c289ff7..93bda306 100644
--- a/partner/dpi/gstreamer/CMakeLists.txt
+++ b/partner/dpi/gstreamer/CMakeLists.txt
@@ -424,19 +424,20 @@ endif ()
 #    set_source_files_properties(${NO_CAST_QUAL} PROPERTIES COMPILE_FLAGS "${CMAKE_CXX_FLAGS} /wd4133")
 #endif ()
 
-#if (GST_VIDEO_RENDERING STREQUAL "gl")
-#  list(APPEND SOURCES VideoSinkGStreamer.cpp)
-#  list(APPEND LIBRARIES gstgl-1.0 )
-#  add_definitions(-DGST_GL)
-#  if (GSTREAMER_VERSION STREQUAL "1.14.4")
-#    add_definitions("-DGSTREAMER_VERSION_1_14")
-#  endif()
-#  if(NRDP_REFERENCE)
-#    add_definitions("-DDPI_REFERENCE_VIDEO_RENDERER_GLES2_GLX")
-#  else()
-#    add_definitions("-DDPI_REFERENCE_VIDEO_RENDERER_GLES2_EGL")
-#  endif()
-# endif()
+if (GST_VIDEO_RENDERING STREQUAL "gl")
+  list(APPEND SOURCES VideoSinkGStreamer.cpp)
+  list(APPEND LIBRARIES gstgl-1.0 )
+  add_definitions(-DGST_GL)
+  include_directories($ENV{STAGING_DIR}/usr/lib/gstreamer-1.0/include/)
+  if ((GSTREAMER_VERSION VERSION_EQUAL "1.14.4") OR (GSTREAMER_VERSION VERSION_GREATER "1.14.4"))
+    add_definitions("-DGSTREAMER_VERSION_1_14")
+  endif()
+  if(NRDP_REFERENCE)
+    add_definitions("-DDPI_REFERENCE_VIDEO_RENDERER_GLES2_GLX")
+  else()
+    add_definitions("-DDPI_REFERENCE_VIDEO_RENDERER_GLES2_EGL")
+  endif()
+endif()
 
 if (GST_VIDEO_RENDERING STREQUAL "westeros")
   add_definitions(-DWESTEROS_SINK)
