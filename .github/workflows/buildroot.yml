name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run buildroot
      run: |
        make nanopi_neo_defconfig
        make
        
